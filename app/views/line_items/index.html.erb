
  <h1>Line Items</h1>

<table>
  <thead>
    <tr>
      <th>Product</th>
      <th>Cart</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= @order.line_item.each do |line_item| %>
      <tr>
        <td><%= line_item.product.price %></td>
        <td><%= line_item.product.quantity %></td>
        <td><%= line_item.product.price %></td>
        <td><%= link_to 'Show', line_item %></td>
        <td><%= link_to 'Edit', edit_line_item_path(line_item) %></td>
        <td><%= link_to 'Destroy', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<br>
<br>
    <%= link_to 'Sigue comprando', products_path, class: 'button is-warning add-to-cart' %>
